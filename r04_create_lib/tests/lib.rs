#[cfg(tests)]
mod tests {
    //extern crate local_lib;
    #[test]
    fn it_works() {
        assert_eq!(2 + 2, 4);
    }
    /*
    #[test]
    fn lib_test_first() {
        assert_eq!(1, local_lib::func_lib::first_lib_function());
    }
    #[test]
    fn lib_test_Second() {
        assert_eq!(2, local_lib::func_lib::second_lib_function());
    }
    #[test]
    //#[should_panic]
    //#[ignore]
    fn lib_first_test() {
        assert_eq!(1,local_lib::func_lib::first_lib_function())
    }
    */
}